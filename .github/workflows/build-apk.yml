name: Build Android APK

on:
  push:
    branches:
      - main
    paths:
      - 'fyp/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Install dependencies
        working-directory: ./fyp
        run: flutter pub get

      - name: Build APK
        working-directory: ./fyp
        run: flutter build apk --release

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: HealthVerse-Android-App
          path: fyp/build/app/outputs/flutter-apk/app-release.apk
