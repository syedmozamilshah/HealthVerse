# This file configures the analyzer, which statically analyzes Dart code to
# check for errors, warnings, and lints.
#
# The issues identified by the analyzer are surfaced in the UI of Dart-enabled
# IDEs (https://dart.dev/tools#ides-and-editors). The analyzer can also be
# invoked from the command line by running `flutter analyze`.

# The following line activates a set of recommended lints for Flutter apps,
# packages, and plugins designed to encourage good coding practices.
include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    # Noisy deprecation introduced by newer Flutter; existing UI code still works.
    deprecated_member_use: ignore
    # Style suggestions — not real bugs; silencing to keep analyze output clean.
    prefer_const_constructors: ignore
    prefer_const_literals_to_create_immutables: ignore
    prefer_const_declarations: ignore
    prefer_final_fields: ignore
    # Hints about BuildContext across async gaps — guarded by `mounted` checks
    # throughout the codebase; downgrade to avoid noisy output.
    use_build_context_synchronously: ignore
    # Harmless style/lint hints we don't want gating builds.
    avoid_print: ignore
    avoid_function_literals_in_foreach_calls: ignore
    unnecessary_brace_in_string_interps: ignore
    unnecessary_string_interpolations: ignore
    curly_braces_in_flow_control_structures: ignore
    no_leading_underscores_for_local_identifiers: ignore

linter:
  rules:
    # avoid_print: false  # Uncomment to disable the `avoid_print` rule
    # prefer_single_quotes: true  # Uncomment to enable the `prefer_single_quotes` rule

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options
